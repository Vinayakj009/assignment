version: "3.1"
services:
  nodejs:
    ports:
      - 8000:8000
    build:
      context: ./docker_images/
      dockerfile: node_image
    image: assignment
    volumes:
      - ./.npm:/root/.npm/
      - ./:/code
    depends_on:
      - mongo
    env_file:
      - ./.env
    command: npm run start
  mongo:
    image: mongo
    restart: always
    volumes:
      - ./volumes/mongoData/debug:/data/db
    env_file:
      - ./.env
  mongo-express:
    image: mongo-express
    ports:
      - 8081:8081
    restart: always
    env_file:
      - ./.env
